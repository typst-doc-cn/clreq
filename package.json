{
    "name": "clreq-gap-typst",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "description": "Chinese Layout Gap Analysis for Typst",
    "dependencies": {
        "glob-watcher": "^6.0.0"
    },
    "scripts": {
        "build": "tsc && vite build && node --experimental-strip-types scripts/compile.ts",
        "dev": "node --experimental-strip-types scripts/compile.ts --watch",
        "preview": "vite preview --base=/clreq/",
        "check": "node --experimental-strip-types scripts/check_uniq.ts"
    },
    "devDependencies": {
        "@types/node": "^24.0.3",
        "typescript": "~5.8.3",
        "vite": "^6.3.5"
    }
}